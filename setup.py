from setuptools import setup, find_packages

setup(
    name="dash-omero-project",
    version="0.1.0",
    description="An dashboard to visualize microscope-metrics data in OMERO",
    packages=find_packages(),
    keywords=["omero"],
)
