from setuptools import setup, find_packages

setup(
    name="omero-dash-webapp",
    version="0.1.0",
    description="OMERO example minimal web app",
    packages=find_packages(),
    keywords=["omero"],
)
